import 'package:flutter/material.dart';

class PetBoardingPage extends StatelessWidget {
  const PetBoardingPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
