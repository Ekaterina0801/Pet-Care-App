import 'package:flutter/material.dart';

class FeedingCalendarPage extends StatelessWidget {
  const FeedingCalendarPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
